The drone must choose which direction to move based on the goal: **"{task}"**.
Take also in consideration an hint (if present) of what to focus on in the choice: **"{hint}"**.
Ignore the hint if is not useful or feasible with the given images.
Take also in consideration the boundaries, answering only with a direction and a distance that are feasible with them.

Here are the 'boundaries':
{boundaries}

Here is the 'current position': {current_position}

You are given 8 images representing what the drone sees in each direction.

Think carefully about:
1. Whether any view shows the **target object**, or **objects semantically related** to the goal (e.g., if the task is to find a banana, seeing a fruit bowl or a kitchen is relevant).
2. Which direction looks most promising based on **relevant objects, open space, or environmental clues**.
3. Avoid directions that are visually blocked, cluttered, or irrelevant.

What is more, based on the 8 images, name the current region where the drone is.

**Return the best direction** among: `North`, `North-East`, `East`, `South-East`, `South`, `South-West`, `West` or `North-West`. What is more return the distance in cm that you consider the best to move. Also include a **short explanation** (1 sentence) of why that direction is best for the given task and why that distance.
Respond in JSON format with the fields 'direction', 'distance', 'reason' and 'region_name'.